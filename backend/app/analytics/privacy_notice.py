PRIVACY_NOTICE = """
Application Finder uses privacy-preserving analytics to understand aggregate usage.
The internal access log does not store names, raw IP addresses, or raw user-agent
strings by default. Anonymous visitor hashes are derived with HMAC-SHA256 and a
daily date component so they are non-reversible and rotate over time. The app
does not use invasive fingerprinting or cross-site tracking.
"""
